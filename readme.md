# tagsystem

## 使用方法
```bash
tman init $PATH     #you need init path in you select path
tap $TAG+ $OBJECT   #add tag in object which you what to tag
tshow               #show a tag
tshow -o $OBJECT    #show tag in a specical object
tas $TAG+           #search by tag (include you give tag)
```